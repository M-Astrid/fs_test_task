{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <div class="">
        <h5>User: {{ app.user.username }} </h5>
        <a href="{{ path('logout') }}"><button class="btn btn-primary"> Logout </button></a>
        {#<h4>Passed tests: {{ app.user.username }} </h4>#}
</div>


        {#<li><a href="{{ path('passed_test_list') }}">Watch passed tests</a></li>#}
</div>
    <div class='container text-center'>
        <h4 class="text-center">Check your knowledge</h4>
        <div class="btn-size">
            <a href="{{ path('test_list') }}"><button class="btn btn-danger btn-lg"> Start a new test </button></a>
            <a href="{{ path('completed_list') }}"><button class="btn btn-primary btn-lg btn-main-menu">Watch passed tests</button></a>
        </div>
    </div>
{% endblock %}
